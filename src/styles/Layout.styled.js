import styled from "styled-components";
const CenterContainer = styled.div`
  margin: auto;
  max-width: var(--max-width);
`;
const MarginWrapper = styled.div`
  margin: 15px;
`;

export { MarginWrapper, CenterContainer };
